#!/usr/bin/env bun

import { main } from "../src/cli.ts";

const args = process.argv.slice(2);
process.exitCode = await main(args.length === 0 ? ["pane"] : args, {
  cwd: process.cwd(),
  env: process.env,
  stdout: process.stdout,
  stderr: process.stderr,
});
